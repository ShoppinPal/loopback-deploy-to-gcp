version: 1.0.0
id: container-vm-loopback
containers:
  - name: loopback
    image: gcr.io/<project_name>/loopback-deploy-to-gcp
    ports:
      - name: www
        hostPort: 80
        containerPort: 80
